apiVersion: v1
kind: Service
metadata:
  name: cicd-lab-webapp-service
spec:
  type: NodePort
  selector:
    app: cicd-lab-webapp
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 32000
